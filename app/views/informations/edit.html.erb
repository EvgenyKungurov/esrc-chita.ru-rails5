<% provide(:title, 'Редактирование статьи') %>
<h1>Редактирование статьи</h1>
 
<%= form_for :information, url: information_path(@information), method: :patch do |f| %>
  <% if @information.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@information.errors.count, "error") %> prohibited
      this information from being saved:</h2>
    <ul>
    <% @information.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
   <%= f.cktext_area :title %>
   <%= f.cktext_area :text %>
   <%= f.submit :"Сохранить" %>
<% end %>
<%= link_to 'Назад', informations_path %>
